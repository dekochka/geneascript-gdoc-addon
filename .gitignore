.DS_Store
*.log
node_modules/
test-results/
playwright-report/

# E2E auth session (contains Google cookies — never commit)
e2e/.auth/

# Local reference videos (e.g. in docs/)
*.mp4
*.mov

# Claude Code per-user overrides (permissions etc.) — shared settings.json IS committed
.claude/settings.local.json
